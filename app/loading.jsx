'use client'
import Image from 'next/image';

const Loading = () => (
  <section className="bg-white w-full min-h-[60vh] lg:min-h-screen flex justify-center items-center">
    <p>Loading...</p>
  </section>
);
export default Loading;